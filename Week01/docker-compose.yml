version: '3'
services:
    web:
        image: nginx:latest
        ports:
            - "80:80"
        volumes:
            - ./nginx.conf:/etc/nginx/conf.d/nginx.conf
            - ./phpmotors:/phpmotors
        php:
          image: php:fpm
          volumes: 
            - ./phpmotors:/phpmotors